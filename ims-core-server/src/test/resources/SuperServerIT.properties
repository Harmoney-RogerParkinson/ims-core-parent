salesforce.url=https://test.salesforce.com
salesforce.username=roger.parkinson@harmoney.co.nz.intsb
salesforce.password=JKBWD0U7JFT7
salesforce.security.token=Ta39svSTj43Ca2JAluHiQRN0
salesforce.authEndpoint=https://test.salesforce.com/services/Soap/u/38.0
#
# If you comment out the following section and uncomment the following section
# the test will save its data in the postgres database rather than using the H2 database
database.dialect:org.hibernate.dialect.H2Dialect
database.datasource.class:org.h2.jdbcx.JdbcDataSource
database.url:jdbc:h2:mem:ims;DB_CLOSE_DELAY=-1;MVCC=true
database.user=
database.password=
database.hbm2ddl.auto=create-drop
#
#database.hbm2ddl.auto=create # this will keep the data in the db after running
#

# If true the test suppresses fetching the Account and Inventment Order records
# and just processes the transaction records.
com.harmoney.ims.core.server.test.SuperServerIT.transactionsOnly=false

# If there is a file given here eg /tmp/myfile.xml then
# the test will dump the database to that before exiting.
com.harmoney.ims.core.server.test.SuperServerIT.saveData=

#salesforce.replayFrom=-1
#salesforce.timeout=5
